# Dependencies (فولدر المكتبات المرفوعة)
node_modules/
/node_modules

# Environment Variables (ملفات المتغيرات البيئية الحساسة)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Config Folder Notes
# ملحوظة: لو عاوز تمنع ملف الـ .env اللي جوه الـ config بس، اكتب المسار بتاعه كدة:
src/common/config/.env

# Production Build Outputs (لو هتعمل بيلد للكود مستقبلاً)
dist/
build/

# Debug logs (ملفات التقارير بتاعة الأخطاء)
npm-debug.log*
yarn-debug.log*
yarn-error.log*